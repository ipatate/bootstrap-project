### [WIP] Bootstrap project base for construct js application ###

The base config use:
* webpack 2
* babel
* sass/scss
* postcss

For dev:
* editorconfig file
* eslint (airbnb-base)
* stylelint (airbnb config)

No production webpack config for the moment.